/*
$(function() {

	var $projects = $("#ui-start-screen-list li"), 
			length = $projects.length, 
			random = Math.floor(Math.random() * (0 - length) + length);

	
	$(window).bind( "load", function() {

			//console.log( "loaded", length, random );

			console.log( "loading ", $projects.eq( random ).text() );

			$projects.eq( random ).trigger( "click" );

	});

});
*/
